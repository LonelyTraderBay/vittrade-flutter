---
description: Cursor subscription workflow — Auto model, batching, doc loading
alwaysApply: true
---

# VitTrade Cursor Workflow

## Model policy (Auto only)

- Always use Cursor **Auto** — do not suggest switching to Sonnet, Opus, thinking, or any specific model tier.
- When a task is hard or blocked: split into **smaller batches**, use GitNexus `impact`/`query`, or Plan mode — **do not** escalate model.
- Use Tab/Apply for micro-edits; Agent for multi-file batches.

## Session discipline

- Tasks spanning **>~10 files** or unclear scope: follow
  [Two-Phase-Cursor-Workflow.md](../../docs/01_AI_RULES/Two-Phase-Cursor-Workflow.md)
  (Plan chat → approve → one Execute chat per batch). Do not escalate model.
- Batch at most **5–10 files** per turn for migration work.
- Start a **new chat** after each completed batch to limit context growth
  (soft ceiling ~40% context for intelligence-sensitive work).
- After a failed attempt, prefer rewind / fresh prompt over stacking fixes
  in a polluted context. Before PR: challenge (“prove this works”) and cite
  verify evidence — not vibes.
- Parallel batches: separate git worktrees; never two writers on overlapping
  files in one tree.
- Load docs **on demand** only — see [docs/INDEX.md](../../docs/INDEX.md). Never load backlog + plan + prompt in one turn.
- Read large files with line limits; do not paste full audit CSVs into chat.
- Codex counterpart: `.codex/skills/planning-and-task-breakdown/SKILL.md`,
  `.codex/skills/incremental-implementation/SKILL.md`, and
  `.codex/skills/vittrade-batch-gate/SKILL.md`.

## Batch completion gate

Before marking a batch complete (do not ask the user first):

1. Scan the batch diff for local widgets duplicating `Vit*`, single-layer wrappers, one-caller helpers, and new pub imports not required by the plan.
2. Trim inline when safe; keep items the plan, tests, or financial safety require.
3. Run verification per [AI_PROMPT_SHELL.md](../../docs/01_AI_RULES/AI_PROMPT_SHELL.md) § Verification gate (`flutter analyze` + focused tests); cite evidence in the completion message.
4. Only then report batch complete. If bloat remains, fix in the same turn.

Self-check format: [.codex/skills/vittrade-minimal-review/SKILL.md](../../.codex/skills/vittrade-minimal-review/SKILL.md).

## Daily startup

```powershell
.\scripts\Start-CursorSession.ps1
```

Or manually: `.\scripts\headroom\Start-VitTradeHeadroom.ps1` then verify in **Cursor Settings → Tools & MCP**:
- **Home MCP**: `gitnexus` + `dart` + `headroom` Connected (`~/.cursor/mcp.json`)
- **Workspace MCP**: empty (project `.cursor/mcp.json` defines no servers)

## Authority

[AGENTS.md](../../AGENTS.md) wins over this file for product, financial, and architecture rules.

## Codex skill routing

For the same procedures when working in Codex, load the smallest matching
skill: planning/incremental implementation for batches, `vittrade-ui-checklists`
for UI, `vittrade-design-domain` for audit selection, `vittrade-minimal-review`
for diff trimming, `vittrade-button-wiring-audit` for dead handlers, and
`code-review-and-quality` before merge.

## Optional QA (gstack)

Install once: `.\scripts\Setup-GStackBrowse.ps1` (global `~/.cursor/skills/gstack-*/`).

| Skill | When |
| --- | --- |
| `/browse` | Visual QA after UI batch — run `flutter run -d chrome` first |
| `/review` | Pre-merge supplement to `code-review-and-quality` |

**Token rule:** use gstack in a **separate QA chat**, not migration batches.

Do **not** invoke gstack plan/design skills (`plan-ceo-review`, `design-shotgun`) —
they conflict with VitTrade execution prompts.
