# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
flutter_app/.dart_tool/
flutter_app/build/
flutter_app/run-artifacts/*
!flutter_app/run-artifacts/README.md
# page-rhythm-l3 là INPUT của artifact committed (screen-compliance CSV đọc
# evidence.csv khi rollup) — phải track để CI regen ra đúng nội dung.
!flutter_app/run-artifacts/page-rhythm-l3/
flutter_app/tmp/
flutter_app/*.log

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Local linked worktrees (never commit gitlinks / machine paths)
.worktrees/

# Claude Code — track harness; ignore runtime / machine-local only
.claude/worktrees/
.claude/data/
.claude/settings.local.json
.claude/**/*.lock
.claude/scheduled_tasks.json
.claude/agent-registry.json
.claude/first-run
.claude/assistant-daemon-state.json
.claude/checkpoints/
.claude/mailbox/
.claude/routines/
CLAUDE.local.md
scripts/headroom/vittrade.headroom.local.env
.headroom/
.tokensave/
.gitnexus/
.kilo/
# Superpowers / SDD agent scratch (briefs, reports, review diffs) — never commit
.superpowers/

# Root-level scratch files
/tmp_*

# Python bytecode caches from local AI/tool scripts
__pycache__/
*.py[cod]

# Retired web tooling / generated artifacts
node_modules/
dist/
.vite/
output/
*.log

# AgentsRoom — machine-local MCP + caches (absolute paths; never commit)
# Home MCP stays in ~/.cursor/mcp.json; workspace .cursor/mcp.json stays empty.
.mcp.json
.agentsroom/
